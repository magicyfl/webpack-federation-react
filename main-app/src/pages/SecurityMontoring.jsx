import React from 'react';
import { useSelector, useDispatch } from 'react-redux';

export default function SecurityMontoring() {
    const data = useSelector(data => data);

    return (
        <div>
            <h2>main-app SecurityMontoring</h2>
        </div>
    )
}